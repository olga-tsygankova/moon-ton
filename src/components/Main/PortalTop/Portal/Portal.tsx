import {Beam} from "../Beam/Beam";
import {LineMain} from "../LineMain/LineMain";


export const Portal= () => {
    return (
        <div className="portal">
            <LineMain/>
            <Beam/>
        </div>
    );
};

